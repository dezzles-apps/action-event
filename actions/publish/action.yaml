name: Post A Build Event
description: Publishes the project to Portfolio
inputs:
  credentialsJson:
    description: The credentials JSON for Build Engine
    required: true
  version:
    description: The version to publish
    required: true

runs:
  using: node24
  main: ../../dist/publish.js
